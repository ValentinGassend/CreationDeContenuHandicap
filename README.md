# CreationDeContenuHandicap

## Structure du projet

```shell

# dossier de travail: CreationDeContenuHandicap

.                   # Fichiers markup (index, 404, etc.)
├── js              # Tous les fichiers js du projet
├── scss            # Fichiers de style
│  ├── blocks       # Style de blocks (slider, gallery, etc.)
│  └── components   # Style de composants (header, footer, navbar, button, etc.)
│  └── variables    # Style de variable (FirstColor, SecondColor, ect.)
└── static
   └── assets       # Style compilé, images, icônes et médias en tout genre (pdf, vidéos, etc.)
      ├── css
      ├── icons
      └── medias

```
